Config = Config or {}

Config.Context = {
    debugMode = false,
    maxVisibleOptions = 10,
    defaultPosition = 'top-right',
    animation = 'slide',
    closeOnSelect = true,
    closeOnBack = true
}

Config.MenuDefaults = {
    title = 'Actions',
    canClose = true,
    onClose = nil,
    onBack = nil
}
